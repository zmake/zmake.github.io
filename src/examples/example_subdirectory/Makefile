# Generated by zmake version 0.2.0, Aug 31th 2016

ZBUILD = ../../zbuild
include $(ZBUILD)/zmake_top.mk

PCH = _example_subdirectory.h


SRC += \
   config_all/Example_All_Subdirectory \

ifeq ($(CONFIG),debug)
SRC += \
   config_debug/Example_Debug_Subdirectory \

endif

ifeq ($(CONFIG),release)
SRC += \
   config_release/Example_Release_Subdirectory \

endif

OR_COND = false
ifeq ($(CONFIG),debug)
OR_COND = true
endif
ifeq ($(CONFIG),thirdexample)
OR_COND = true
endif
ifeq ($(OR_COND),true)
SRC += \
   config_two/Example_Two_Subdirectory \

endif

SRC += \
   plat_osx/Example_Osx_Subdirectory \

include $(ZBUILD)/zmake_base.mk
