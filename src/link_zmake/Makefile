# Generated by zmake version 0.2.0, Aug 31th 2016

ZBUILD = ../zbuild
include $(ZBUILD)/zmake_top.mk

PCH = _link_zmake.h


SRC += \
   ZmakeLink \


OR_COND = false
ifeq ($(CONFIG),debug)
OR_COND = true
endif
ifeq ($(OR_COND),true)
PROJS += \
   examples/example \

endif
PROJS += \
   examples/example_subdirectory \
   wton \
   zmake \

TARGETROOT = src/zbuild/zmake/
TARGET = zmake

include $(ZBUILD)/zmake_base.mk
